#include<iostream>
using namespace std;
int main()

{
	int num;
	cout<<"enter the value of num :";
	//decleration of the number
	//now input the number from the user
	cin>>num;
	//because prime number are those which multpliy by itself to return that number
	if(num*num==num)
	{
		cout<<"the number is prime :";
	}
	else
	
	cout<<"the number is not prime";
}
